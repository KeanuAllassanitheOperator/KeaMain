version: "3.5"

services:
  grafana8:
    image: grafana/grafana:latest
    container_name: grafana8
    volumes:
      - grafana-data:/var/lib/grafana
    user: 0:0
    ports:
      - "3010:3000"
    restart: always

    volumes:
      grafana-data:
